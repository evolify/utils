# `@evolify/dom-utils`

> dom utils.

## Usage

```js
import { className, getCookie } from "@evolify/dom-utils"

// className
const disable = false
className("item", disable && "disabled") // "item disabled"

// getCookie
getCookie("uid")
```
